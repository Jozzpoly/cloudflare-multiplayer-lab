name: WS0 Tick Domain T2 Snapshot Age

on:
  pull_request:
    paths:
      - "scripts/ws0-tick-domain-t0.mjs"
      - "scripts/ws0-tick-domain-t1-actor-state.mjs"
      - "scripts/ws0-tick-domain-t2-snapshot-age.mjs"
      - ".github/workflows/ws0-tick-domain-t2-snapshot-age.yml"
      - "package.json"
      - "package-lock.json"

permissions:
  contents: read

jobs:
  tick-domain-t2-snapshot-age:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Validate T0/T1/T2 apparatus
        run: |
          node --check scripts/ws0-tick-domain-t0.mjs
          node --check scripts/ws0-tick-domain-t1-actor-state.mjs
          node --check scripts/ws0-tick-domain-t2-snapshot-age.mjs

      - name: Re-run T0 control
        run: WS0_TICK_T0_OUTPUT=ws0-tick-domain-t0-regression.json node scripts/ws0-tick-domain-t0.mjs

      - name: Re-run T1 fresh-state envelope
        run: WS0_TICK_T1_OUTPUT=ws0-tick-domain-t1-regression.json node scripts/ws0-tick-domain-t1-actor-state.mjs

      - name: Run T2 snapshot-age / forecast envelope
        run: node scripts/ws0-tick-domain-t2-snapshot-age.mjs

      - name: Preserve T0/T1/T2 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ws0-tick-domain-t2-snapshot-age
          path: |
            ws0-tick-domain-t0-regression.json
            ws0-tick-domain-t1-regression.json
            ws0-tick-domain-t2-snapshot-age.json
          if-no-files-found: ignore
