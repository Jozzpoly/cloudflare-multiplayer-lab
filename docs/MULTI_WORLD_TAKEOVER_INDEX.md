# Multi_World — Takeover package index

Status: **HANDOFF READY / PRE-OWNER FALSIFICATION AUDIT APPLIED / R0d HUMAN GATE NEXT**  
Updated: **2026-09-07**  
Handoff branch: `multi-world-r0d-reliability-handoff`

This index supersedes the old A2R-era takeover order and the earlier pre-audit R0d startup order.

The handoff branch is documentation/reference only. Runtime authority remains the frozen source and delivery specimens listed below.

---

## Canonical reading order now

1. **`MULTI_WORLD_CURRENT_STATE.md`**  
   Baseline current-state authority from the machine-qualification closure. Read its runtime/delivery anchors, but apply the later pre-Owner audit corrections below.

2. **`WORLD_V0_R0D_PRE_OWNER_FALSIFICATION_AUDIT_2026-09-07.md`**  
   Current pre-human truth for procedure and evidence boundaries. It red-teams I1–I4, the delivery apparatus, mobile lifecycle assumptions and the human gate. It corrects the overlong run-ID mistake and narrows same-page ActorSession resume versus reload/process-loss behavior.

3. **`MULTI_WORLD_FRESH_TAKEOVER_V3.md`**  
   Current startup mandate for a fresh Browser ChatGPT continuation. V3 supersedes V2.

4. **`WORLD_V0_R0D_RELIABILITY_RETEST_HANDOFF.md`**  
   Detailed machine/handoff history. Still useful, but its broad `leave/rejoin` wording and unconstrained fresh-run example are superseded by the pre-Owner audit.

5. **`WORLD_V0_R0D_RELIABILITY_EVIDENCE_MANIFEST.md`**  
   Preserved exact digest of final remote workflow artifact identity and compact I1/I2/I3/I4b outputs.

6. **GitHub issue #8, comment `5558411044`**  
   Original R0d human continuity FAIL that falsified the old lifecycle model.

7. **GitHub issue #8, comment `5562283164`**  
   Final isolated remote machine gate GREEN after I1–I4.

8. **Newest issue #8 pre-Owner audit checkpoint**  
   Records the corrected human-gate contract and final audit verdict. Prefer it over the earlier handoff closure when procedure differs.

9. **`MULTI_WORLD_PROJECT_SOUL.md`**  
   Stable broader project intent when needed.

Older `MULTI_WORLD_FRESH_TAKEOVER_V2.md`, V1 and A2R-era grounding remain provenance, not current startup authority.

---

## Exact frozen runtime/delivery anchors

### Qualified product + reliability source

`world-v0-multiplayer-foundation-integration@a2e821afbbc88371b033af311cc6882d46aa6916`

Role: frozen R0c-product + Multiplayer Reliability Foundation I1–I4 runtime control.

### Isolated R0d delivery specimen

`world-v0-r0d-reliability-retest@7da9ddd4ad37221f63a3cd418a140824783480ec`

Role: delivery-only descendant of `a2e821...`.

Expected isolated Worker:

`cloudflare-multiplayer-lab-reliability-play`

Final remote workflow:

`34060903778` — **PASS**

Delivery diff from `a2e821...` remains limited to:

- `.github/workflows/world-v0-r0d-reliability-retest.yml`;
- `wrangler.jsonc` isolated `reliability_play` environment.

No qualified World V0 product/runtime bytes differ.

### Handoff/documentation branch

`multi-world-r0d-reliability-handoff`

Its head may move only for documentation/evidence continuity. It is not a runtime candidate.

---

## Current evidence boundary after falsification audit

The first real R0d test failed because one actor starvation/socket loss could kill the whole WorldEpoch.

The audit reverified that the qualified active-run server now removes that coupling causally:

- ActorSession lifetime is independent from current WebSocket lifetime;
- one actor lease expiry becomes actor-local neutral containment;
- one actor transport loss does not globally end the epoch;
- same-page resume can rebind the ActorSession using its private token;
- long gaps can recover through exact authority full-state seed/rebase.

Machine evidence remains strong:

- I1 ActorSession continuity — PASS;
- I2 future-intent supersession — PASS;
- I3b clean 1200 ms rAF freeze — PASS;
- I4b exact full-state rebase after 1500 ms controlled outage / 170-tick gap, healthy peer live, exact guards — PASS.

The audit also preserved important nonclaims:

- same-page resume is **not** persistent player identity across reload/process destruction;
- I3b/I4b are not proof of complete real-mobile background/freeze/discard behavior;
- browser explicit-close auto-resume is narrower than all possible WebSocket close codes;
- Durable Object/Worker process-loss world persistence remains unimplemented;
- pre-start waiting-peer loss intentionally remains fail-closed.

The Owner human test remains the highest-information next evidence.

---

## Correct next human action

Do **not** reuse any run ID already written in an older conversation.

At actual test time:

1. generate a new high-entropy run ID matching `^[A-Za-z0-9_-]{1,20}$`;
2. do not pre-touch that exact run remotely or through CI;
3. use the isolated Worker deep link;
4. Owner in Poland opens it and **clicks Enter first**;
5. wait until Owner reaches `waiting for peer`;
6. only then share the exact same URL with the second real device/person;
7. begin ordinary two-person play;
8. use one natural second-device background -> foreground event as the primary continuity disturbance.

Do not deliberately full-close/reload the second page as the first R0d disturbance. Cross-page ActorSession persistence is not the current qualified contract.

Primary question:

> Is the previous `live -> blank/recovery/waiting -> live` **global** continuity failure gone, with the healthy peer/world remaining continuously alive while the interrupted same-page actor recovers?

If natural mobile background itself causes discard/reload and token loss, preserve it as meaningful new product evidence rather than declaring the test invalid.

---

## Scope freeze

Do not add I5, client-session persistence, Durable Object persistence, broader reconciliation, content/jump work, 3-player expansion, lobby redesign, broad refactoring or another generic synthetic matrix before the Owner verdict merely because the audit identified future edges.

The audit verdict is **READY FOR OWNER TEST WITH CORRECTED PROCEDURE**, not authorization for pre-emptive expansion.

---

## Preserved artifact identity

Final GitHub Actions artifact from workflow `34060903778`:

- artifact ID `9997448698`;
- artifact name `world-v0-r0d-reliability-retest-1`;
- ZIP size `54,384 bytes`;
- ZIP SHA-256 `2a2a740d9fe3e9f87602a1704814ef824f0fb716ec8e8c79d1194e441d3942b4`.

`WORLD_V0_R0D_RELIABILITY_EVIDENCE_MANIFEST.md` preserves the exact file-level hashes and compact verdict payloads.

---

## Historical/non-frontier warning

`world-v0-friend-ready-foundation-integration@5dd28a899c4f60c9227f1eb93026f571ced733e3`

remains an abandoned inert branch from a rejected ancestry hypothesis. It received no new runtime work and is not a frontier.
