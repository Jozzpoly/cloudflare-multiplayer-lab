{
  "name": "cloudflare-multiplayer-lab",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env staging",
    "deploy": "npm run deploy:staging",
    "deploy:staging": "node scripts/guard-staging-deploy.mjs && wrangler deploy --env staging",
    "deploy:production": "wrangler deploy",
    "generate:types": "wrangler types --env staging",
    "typecheck": "npm run generate:types && tsc --noEmit",
    "check:client": "node --check public/app.js && node --check public/world0/app.js && node --check public/world0-a2r/fixed-step-clock.js && node --check public/world0-a2r/app.js && node --check public/world0-two-client/app.js && node --check public/world0-two-client/touch-controls.js",
    "lab:a2r": "node scripts/a2r-forecast-lab.mjs",
    "check:a2r": "node scripts/a2r-forecast-lab.mjs --ci",
    "validate:staging-contract": "node scripts/validate-staging-contract.mjs",
    "test:staging-deploy-guard": "node scripts/test-staging-deploy-guard.mjs",
    "validate:worker": "wrangler deploy --dry-run --env staging --outdir .wrangler/dry-run",
    "check": "npm run validate:staging-contract && npm run test:staging-deploy-guard && npm run check:client && npm run check:a2r && npm run typecheck && npm run validate:worker"
  },
  "dependencies": {
    "box3d.js": "0.1.1"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "4.127.1"
  },
  "engines": {
    "node": ">=22"
  }
}
