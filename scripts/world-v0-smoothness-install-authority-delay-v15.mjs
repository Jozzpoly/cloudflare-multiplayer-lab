import { readFileSync, writeFileSync } from "node:fs";

const path = "public/world-v0/app.js";
const marker = "WORLD_V0_AUTHORITY_DELIVERY_DELAY_V15";
let source = readFileSync(path, "utf8");
if (source.includes(marker)) {
  console.log("authority-delivery delay v15 hook already installed");
  process.exit(0);
}

function replaceOnce(from, to, label) {
  const first = source.indexOf(from);
  if (first < 0) throw new Error(`V15 authority-delay install missing seam: ${label}`);
  if (source.indexOf(from, first + from.length) >= 0) throw new Error(`V15 authority-delay install ambiguous seam: ${label}`);
  source = source.slice(0, first) + to + source.slice(first + from.length);
}

const canonicalHandler = `  connection.addEventListener("message", (event) => {\n    if (socket !== connection || typeof event.data !== "string") return;\n    try {\n      const message = JSON.parse(event.data);\n      const observedBoundary = Number.isInteger(message?.boundaryTick)\n        ? message.boundaryTick\n        : (Number.isInteger(message?.state?.boundaryTick) ? message.state.boundaryTick : null);\n      if (Number.isInteger(observedBoundary)) {\n        lastAuthorityBoundaryTick = lastAuthorityBoundaryTick === null\n          ? observedBoundary\n          : Math.max(lastAuthorityBoundaryTick, observedBoundary);\n        metrics.latestAuthorityBoundary = lastAuthorityBoundaryTick;\n      }\n      handleMessage(message);\n      updateProductStatus();\n    } catch (error) { candidateError(error); }\n  });`;

const instrumentedHandler = `  connection.addEventListener("message", (event) => {\n    if (socket !== connection || typeof event.data !== "string") return;\n    let message;\n    let observedBoundary;\n    try {\n      message = JSON.parse(event.data);\n      observedBoundary = Number.isInteger(message?.boundaryTick)\n        ? message.boundaryTick\n        : (Number.isInteger(message?.state?.boundaryTick) ? message.state.boundaryTick : null);\n    } catch (error) {\n      candidateError(error);\n      return;\n    }\n\n    const deliver = () => {\n      if (socket !== connection) {\n        if (typeof window.__mwV15AuthorityDelayDropped === "function") window.__mwV15AuthorityDelayDropped(message);\n        return;\n      }\n      try {\n        if (Number.isInteger(observedBoundary)) {\n          lastAuthorityBoundaryTick = lastAuthorityBoundaryTick === null\n            ? observedBoundary\n            : Math.max(lastAuthorityBoundaryTick, observedBoundary);\n          metrics.latestAuthorityBoundary = lastAuthorityBoundaryTick;\n        }\n        handleMessage(message);\n        updateProductStatus();\n        if (typeof window.__mwV15AuthorityDelayDelivered === "function") window.__mwV15AuthorityDelayDelivered(message, observedBoundary);\n      } catch (error) { candidateError(error); }\n    };\n\n    const delayMs = typeof window.__mwV15AuthorityDelayFor === "function"\n      ? Number(window.__mwV15AuthorityDelayFor(message, observedBoundary) || 0)\n      : 0;\n    if (delayMs > 0) {\n      if (typeof window.__mwV15AuthorityDelayScheduled === "function") window.__mwV15AuthorityDelayScheduled(message, observedBoundary, delayMs);\n      setTimeout(deliver, delayMs);\n      return;\n    }\n    deliver();\n  });`;

replaceOnce(canonicalHandler, instrumentedHandler, "WebSocket authority delivery handler");

source += `\n// ${marker}: test-only delivery control. Canonical app.js is restored after CI.\n{\n  const DELAYABLE_V15 = new Set(["world_v0_peer_records", "world_v0_consumed", "world_v0_batch_ack", "world_v0_snapshot"]);\n  const state = {\n    delayMs: 0,\n    scheduled: 0,\n    delivered: 0,\n    dropped: 0,\n    byType: {},\n    maxObservedBoundary: null,\n    events: [],\n  };\n  const retain = (event) => {\n    state.events.push(event);\n    if (state.events.length > 256) state.events.splice(0, state.events.length - 256);\n  };\n  window.__mwV15SetAuthorityDelay = (delayMs) => {\n    state.delayMs = Math.max(0, Number(delayMs) || 0);\n    retain({ kind: "set-delay", at: performance.now(), delayMs: state.delayMs });\n    return state.delayMs;\n  };\n  window.__mwV15ResetAuthorityDelay = () => {\n    state.scheduled = 0;\n    state.delivered = 0;\n    state.dropped = 0;\n    state.byType = {};\n    state.maxObservedBoundary = null;\n    state.events = [];\n    return true;\n  };\n  window.__mwV15AuthorityDelayFor = (message, observedBoundary) => {\n    if (!(state.delayMs > 0) || !Number.isInteger(observedBoundary)) return 0;\n    return DELAYABLE_V15.has(message?.type) ? state.delayMs : 0;\n  };\n  window.__mwV15AuthorityDelayScheduled = (message, observedBoundary, delayMs) => {\n    state.scheduled += 1;\n    const type = String(message?.type || "unknown");\n    state.byType[type] = (state.byType[type] || 0) + 1;\n    if (Number.isInteger(observedBoundary)) state.maxObservedBoundary = state.maxObservedBoundary === null ? observedBoundary : Math.max(state.maxObservedBoundary, observedBoundary);\n    if (state.scheduled <= 12 || state.scheduled % 50 === 0) retain({ kind: "scheduled", at: performance.now(), type, observedBoundary, delayMs });\n    return true;\n  };\n  window.__mwV15AuthorityDelayDelivered = (message, observedBoundary) => {\n    if (DELAYABLE_V15.has(message?.type)) state.delivered += 1;\n    if (state.delivered <= 12 || state.delivered % 50 === 0) retain({ kind: "delivered", at: performance.now(), type: message?.type || null, observedBoundary });\n    return true;\n  };\n  window.__mwV15AuthorityDelayDropped = (message) => {\n    if (DELAYABLE_V15.has(message?.type)) state.dropped += 1;\n    retain({ kind: "dropped", at: performance.now(), type: message?.type || null });\n    return true;\n  };\n  window.__mwV15ReadAuthorityDelay = () => ({ ...state, byType: { ...state.byType }, events: state.events.map((entry) => ({ ...entry })) });\n}\n`;

writeFileSync(path, source);
console.log("WORLD_V0_AUTHORITY_DELIVERY_DELAY_V15_INSTALLED");
