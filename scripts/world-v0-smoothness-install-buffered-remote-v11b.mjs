import { readFileSync, writeFileSync } from "node:fs";

const path = "public/world-v0/app.js";
const marker = "WORLD_V0_BUFFERED_REMOTE_V11B";
let source = readFileSync(path, "utf8");
if (source.includes(marker)) {
  console.log("buffered remote v11b hook already installed");
  process.exit(0);
}

const remoteSync = `  if (remoteBody && remoteMesh) {\n    const position = bodyPosition(remoteBody);\n    remoteMesh.position.fromArray(position);\n    remoteMesh.quaternion.fromArray(bodyRotation(remoteBody)).normalize();\n    syncPresence(remoteMesh, position);\n  }`;
if (!source.includes(remoteSync)) throw new Error("V11b install missing remote sync seam");
if (source.indexOf(remoteSync) !== source.lastIndexOf(remoteSync)) throw new Error("V11b install ambiguous remote sync seam");
source = source.replace(remoteSync, `${remoteSync}\n  // ${marker}: test-only position presentation isolation. Exact simulation and\n  // quaternion remain untouched; only the visible remote position is replaced.\n  if (remoteBody && remoteMesh && typeof window.__mwV11bApplyRemotePresentation === "function") {\n    window.__mwV11bApplyRemotePresentation(remoteMesh, bodyPosition(remoteBody));\n  }`);

source += `\n// ${marker} public test controls. Requires WORLD_V0_CONFIRMED_TIMELINE_V11.\n{\n  if (typeof window.__mwV11CommitBoundary !== "function" || typeof window.__mwV11ReadConfirmedTimeline !== "function") {\n    throw new Error("V11b requires confirmed timeline V11 controls");\n  }\n  const originalCommitBoundaryV11b = window.__mwV11CommitBoundary;\n  const state = {\n    anchors: [],\n    enabled: false,\n    currentPosition: null,\n    presentationBoundary: null,\n    targetBoundary: null,\n    segmentFromPosition: null,\n    segmentFromBoundary: null,\n    segmentTargetPosition: null,\n    segmentStartedAt: null,\n    segmentDurationMs: STEP_MS * 6,\n    samples: [],\n    segmentEvents: [],\n  };\n  const clone = (value) => Array.isArray(value) ? [...value] : null;\n  const lerp = (a, b, t) => [\n    a[0] + (b[0] - a[0]) * t,\n    a[1] + (b[1] - a[1]) * t,\n    a[2] + (b[2] - a[2]) * t,\n  ];\n  const distance = (a, b) => a && b ? Math.hypot(a[0] - b[0], a[1] - b[1], a[2] - b[2]) : null;\n  const latestAnchor = () => state.anchors.length ? state.anchors[state.anchors.length - 1] : null;\n\n  window.__mwV11CommitBoundary = (boundary, localBoundary) => {\n    const result = originalCommitBoundaryV11b(boundary, localBoundary);\n    if (!result) return result;\n    const snapshot = window.__mwV11ReadConfirmedTimeline();\n    const commitment = [...(snapshot.commitments || [])].reverse().find((entry) => entry.boundary === boundary && Array.isArray(entry.position));\n    if (!commitment) return result;\n    if (state.anchors.some((entry) => entry.boundary === boundary)) return result;\n    state.anchors.push({ boundary, position: clone(commitment.position), committedAt: commitment.committedAt, lagTicks: commitment.lagTicks });\n    state.anchors.sort((a, b) => a.boundary - b.boundary);\n    if (state.anchors.length > 64) state.anchors.splice(0, state.anchors.length - 64);\n    return result;\n  };\n\n  window.__mwV11bCommittedAnchorCount = () => state.anchors.length;\n  window.__mwV11bResetAll = () => {\n    state.anchors = [];\n    state.enabled = false;\n    state.currentPosition = null;\n    state.presentationBoundary = null;\n    state.targetBoundary = null;\n    state.segmentFromPosition = null;\n    state.segmentFromBoundary = null;\n    state.segmentTargetPosition = null;\n    state.segmentStartedAt = null;\n    state.segmentDurationMs = STEP_MS * 6;\n    state.samples = [];\n    state.segmentEvents = [];\n    return true;\n  };\n  window.__mwV11bResetMetrics = () => {\n    state.samples = [];\n    state.segmentEvents = [];\n    return true;\n  };\n  window.__mwV11bEnable = () => {\n    if (state.anchors.length < 2) return false;\n    const from = state.anchors[state.anchors.length - 2];\n    const to = state.anchors[state.anchors.length - 1];\n    state.enabled = true;\n    state.currentPosition = clone(from.position);\n    state.presentationBoundary = from.boundary;\n    state.targetBoundary = to.boundary;\n    state.segmentFromPosition = clone(from.position);\n    state.segmentFromBoundary = from.boundary;\n    state.segmentTargetPosition = clone(to.position);\n    state.segmentStartedAt = performance.now();\n    state.segmentDurationMs = Math.max(STEP_MS, (to.boundary - from.boundary) * STEP_MS);\n    return true;\n  };\n  window.__mwV11bDisable = () => { state.enabled = false; return true; };\n\n  window.__mwV11bApplyRemotePresentation = (mesh, exactPosition) => {\n    if (!state.enabled || !mesh || state.anchors.length < 2) return false;\n    const now = performance.now();\n    const newest = latestAnchor();\n    if (!newest?.position) return false;\n    if (state.targetBoundary !== newest.boundary) {\n      const previousAnchor = state.anchors[state.anchors.length - 2];\n      const fromPosition = clone(state.currentPosition ?? previousAnchor?.position ?? newest.position);\n      const fromBoundary = Number.isFinite(state.presentationBoundary)\n        ? state.presentationBoundary\n        : (previousAnchor?.boundary ?? newest.boundary);\n      state.segmentFromPosition = fromPosition;\n      state.segmentFromBoundary = fromBoundary;\n      state.segmentTargetPosition = clone(newest.position);\n      state.segmentStartedAt = now;\n      state.segmentDurationMs = Math.max(STEP_MS, (newest.boundary - (previousAnchor?.boundary ?? newest.boundary - 1)) * STEP_MS);\n      state.targetBoundary = newest.boundary;\n      state.segmentEvents.push({\n        t: now,\n        fromBoundary,\n        targetBoundary: newest.boundary,\n        durationMs: state.segmentDurationMs,\n        fromPosition: clone(fromPosition),\n        targetPosition: clone(newest.position),\n      });\n      if (state.segmentEvents.length > 512) state.segmentEvents.splice(0, state.segmentEvents.length - 512);\n    }\n    const elapsed = Math.max(0, now - (state.segmentStartedAt ?? now));\n    const alpha = Math.max(0, Math.min(1, elapsed / Math.max(STEP_MS, state.segmentDurationMs)));\n    const displayed = lerp(state.segmentFromPosition, state.segmentTargetPosition, alpha);\n    state.currentPosition = displayed;\n    state.presentationBoundary = state.segmentFromBoundary + (state.targetBoundary - state.segmentFromBoundary) * alpha;\n    mesh.position.fromArray(displayed);\n    syncPresence(mesh, displayed);\n    state.samples.push({\n      t: now,\n      localBoundary: Number.isInteger(localState?.boundaryTick) ? localState.boundaryTick : null,\n      newestCommittedBoundary: newest.boundary,\n      presentationBoundary: state.presentationBoundary,\n      targetBoundary: state.targetBoundary,\n      alpha,\n      exactPosition: clone(exactPosition),\n      presentationPosition: clone(displayed),\n      spatialOffset: distance(displayed, exactPosition),\n    });\n    if (state.samples.length > 4096) state.samples.splice(0, state.samples.length - 4096);\n    return true;\n  };\n\n  window.__mwV11bReadPresentation = () => ({\n    enabled: state.enabled,\n    anchors: state.anchors.map((entry) => ({ ...entry, position: clone(entry.position) })),\n    samples: state.samples.map((entry) => ({ ...entry, exactPosition: clone(entry.exactPosition), presentationPosition: clone(entry.presentationPosition) })),\n    segmentEvents: state.segmentEvents.map((entry) => ({ ...entry, fromPosition: clone(entry.fromPosition), targetPosition: clone(entry.targetPosition) })),\n    currentPosition: clone(state.currentPosition),\n    presentationBoundary: state.presentationBoundary,\n    targetBoundary: state.targetBoundary,\n  });\n}\n`;

writeFileSync(path, source);
console.log("WORLD_V0_BUFFERED_REMOTE_V11B_INSTALLED");
