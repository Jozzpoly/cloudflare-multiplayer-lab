import { readFileSync, writeFileSync } from "node:fs";

const path = "public/world-v0/app.js";
const marker = "WORLD_V0_CONFIRMED_TIMELINE_V11";
let source = readFileSync(path, "utf8");
if (source.includes(marker)) {
  console.log("confirmed timeline v11 hook already installed");
  process.exit(0);
}

function replaceOnce(from, to, label) {
  const first = source.indexOf(from);
  if (first < 0) throw new Error(`V11 install missing seam: ${label}`);
  if (source.indexOf(from, first + from.length) >= 0) throw new Error(`V11 install ambiguous seam: ${label}`);
  source = source.slice(0, first) + to + source.slice(first + from.length);
}

replaceOnce(
  "  diagnosticSamples.set(boundaryTick, capturePackedDiagnostic(localState.sim));",
  `  diagnosticSamples.set(boundaryTick, capturePackedDiagnostic(localState.sim));\n  // ${marker}: test-only remote presentation timeline sample. Replay overwrites\n  // speculative boundaries; a post-commit rewrite is recorded as a violation.\n  if (typeof window.__mwV11RecordBoundary === "function") {\n    const remoteBodyV11 = remoteSessionId ? localState.sim.actorBodies.get(remoteSessionId) : null;\n    window.__mwV11RecordBoundary(boundaryTick, remoteBodyV11 ? bodyPosition(remoteBodyV11) : null);\n  }`,
  "store diagnostic remote position"
);

replaceOnce(
  "  metrics.guardMatches += 1;\n}",
  `  metrics.guardMatches += 1;\n  // ${marker}: a matching authority state guard makes this exact boundary safe\n  // to expose to a confirmed presentation timeline.\n  if (typeof window.__mwV11CommitBoundary === "function") {\n    window.__mwV11CommitBoundary(boundaryTick, localState?.boundaryTick ?? boundaryTick);\n  }\n}`,
  "state guard commit"
);

source += `\n// ${marker} public test controls. Canonical app.js is restored after CI.\n{\n  const state = {\n    samples: new Map(),\n    committed: new Map(),\n    commitments: [],\n    revisionViolations: [],\n    missingCommitSamples: [],\n    frameSamples: [],\n    latestCommittedBoundary: null,\n    samplerRunning: false,\n    samplerGeneration: 0,\n  };\n  const clonePosition = (position) => Array.isArray(position) ? [...position] : null;\n  const distance = (a, b) => {\n    if (!a || !b) return a === b ? 0 : Infinity;\n    return Math.hypot((a[0] ?? 0) - (b[0] ?? 0), (a[1] ?? 0) - (b[1] ?? 0), (a[2] ?? 0) - (b[2] ?? 0));\n  };\n  const trimMap = (map, newest, retain = 512) => {\n    const cutoff = newest - retain;\n    for (const key of map.keys()) if (key < cutoff) map.delete(key);\n  };\n\n  window.__mwV11RecordBoundary = (boundary, position) => {\n    if (!Number.isInteger(boundary)) return false;\n    const next = clonePosition(position);\n    const committed = state.committed.get(boundary);\n    if (committed && distance(committed.position, next) > 1e-7) {\n      state.revisionViolations.push({\n        t: performance.now(),\n        boundary,\n        committedPosition: clonePosition(committed.position),\n        rewrittenPosition: next,\n        distance: distance(committed.position, next),\n      });\n      if (state.revisionViolations.length > 128) state.revisionViolations.splice(0, state.revisionViolations.length - 128);\n    }\n    state.samples.set(boundary, { boundary, position: next, recordedAt: performance.now() });\n    trimMap(state.samples, boundary);\n    return true;\n  };\n\n  window.__mwV11CommitBoundary = (boundary, localBoundary) => {\n    if (!Number.isInteger(boundary)) return false;\n    const sample = state.samples.get(boundary);\n    if (!sample) {\n      state.missingCommitSamples.push({ t: performance.now(), boundary, localBoundary });\n      if (state.missingCommitSamples.length > 128) state.missingCommitSamples.splice(0, state.missingCommitSamples.length - 128);\n      return false;\n    }\n    const existing = state.committed.get(boundary);\n    if (existing) {\n      if (distance(existing.position, sample.position) > 1e-7) {\n        state.revisionViolations.push({\n          t: performance.now(),\n          boundary,\n          committedPosition: clonePosition(existing.position),\n          recommittedPosition: clonePosition(sample.position),\n          distance: distance(existing.position, sample.position),\n          kind: "recommit_changed",\n        });\n      }\n      return true;\n    }\n    const resolvedLocalBoundary = Number.isInteger(localBoundary) ? localBoundary : boundary;\n    const committedSample = {\n      boundary,\n      position: clonePosition(sample.position),\n      committedAt: performance.now(),\n      localBoundary: resolvedLocalBoundary,\n      lagTicks: Math.max(0, resolvedLocalBoundary - boundary),\n    };\n    state.committed.set(boundary, committedSample);\n    state.commitments.push(committedSample);\n    if (state.commitments.length > 2048) state.commitments.splice(0, state.commitments.length - 2048);\n    state.latestCommittedBoundary = state.latestCommittedBoundary === null ? boundary : Math.max(state.latestCommittedBoundary, boundary);\n    trimMap(state.committed, state.latestCommittedBoundary, 1024);\n    return true;\n  };\n\n  window.__mwV11ResetConfirmedTimeline = () => {\n    state.samples.clear();\n    state.committed.clear();\n    state.commitments = [];\n    state.revisionViolations = [];\n    state.missingCommitSamples = [];\n    state.frameSamples = [];\n    state.latestCommittedBoundary = null;\n    state.samplerRunning = false;\n    state.samplerGeneration += 1;\n    return true;\n  };\n\n  window.__mwV11StartSampler = () => {\n    state.frameSamples = [];\n    state.samplerRunning = true;\n    state.samplerGeneration += 1;\n    const generation = state.samplerGeneration;\n    const sampleFrame = (t) => {\n      if (!state.samplerRunning || generation !== state.samplerGeneration) return;\n      const localBoundary = Number.isInteger(localState?.boundaryTick) ? localState.boundaryTick : null;\n      const newest = state.latestCommittedBoundary;\n      state.frameSamples.push({\n        t,\n        localBoundary,\n        newestCommittedBoundary: newest,\n        committedCount: state.committed.size,\n        lagTicks: Number.isInteger(localBoundary) && Number.isInteger(newest) ? Math.max(0, localBoundary - newest) : null,\n      });\n      if (state.frameSamples.length > 4096) state.frameSamples.splice(0, state.frameSamples.length - 4096);\n      requestAnimationFrame(sampleFrame);\n    };\n    requestAnimationFrame(sampleFrame);\n    return true;\n  };\n\n  window.__mwV11StopSampler = () => {\n    state.samplerRunning = false;\n    state.samplerGeneration += 1;\n    return true;\n  };\n\n  window.__mwV11ReadConfirmedTimeline = () => ({\n    commitments: state.commitments.map((entry) => ({ ...entry, position: clonePosition(entry.position) })),\n    revisionViolations: state.revisionViolations.map((entry) => ({ ...entry, committedPosition: clonePosition(entry.committedPosition), rewrittenPosition: clonePosition(entry.rewrittenPosition), recommittedPosition: clonePosition(entry.recommittedPosition) })),\n    missingCommitSamples: state.missingCommitSamples.map((entry) => ({ ...entry })),\n    frameSamples: state.frameSamples.map((entry) => ({ ...entry })),\n    latestCommittedBoundary: state.latestCommittedBoundary,\n    retainedCommittedCount: state.committed.size,\n    retainedSpeculativeCount: state.samples.size,\n  });\n}\n`;

writeFileSync(path, source);
console.log("WORLD_V0_CONFIRMED_TIMELINE_V11_INSTALLED");
