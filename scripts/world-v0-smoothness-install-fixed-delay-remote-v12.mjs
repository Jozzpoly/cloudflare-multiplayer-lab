import { readFileSync, writeFileSync } from "node:fs";

const delayTicks = Number(process.argv[2] ?? 10);
if (!Number.isInteger(delayTicks) || delayTicks < 4 || delayTicks > 24) {
  throw new Error(`V12 delay ticks must be integer 4..24, got ${process.argv[2]}`);
}
const path = "public/world-v0/app.js";
const marker = "WORLD_V0_FIXED_DELAY_REMOTE_V12";
let source = readFileSync(path, "utf8");
if (source.includes(marker)) {
  console.log("fixed-delay remote v12 hook already installed");
  process.exit(0);
}

const remoteSync = `  if (remoteBody && remoteMesh) {\n    const position = bodyPosition(remoteBody);\n    remoteMesh.position.fromArray(position);\n    remoteMesh.quaternion.fromArray(bodyRotation(remoteBody)).normalize();\n    syncPresence(remoteMesh, position);\n  }`;
if (!source.includes(remoteSync)) throw new Error("V12 install missing remote sync seam");
if (source.indexOf(remoteSync) !== source.lastIndexOf(remoteSync)) throw new Error("V12 install ambiguous remote sync seam");
source = source.replace(remoteSync, `${remoteSync}\n  // ${marker}: test-only fixed-tick delayed remote position from immutable\n  // confirmed anchors. Exact simulation, authority and quaternion are untouched.\n  if (remoteBody && remoteMesh && typeof window.__mwV12ApplyRemotePresentation === "function") {\n    window.__mwV12ApplyRemotePresentation(remoteMesh, bodyPosition(remoteBody));\n  }`);

source += `\n// ${marker} public test controls. Requires WORLD_V0_CONFIRMED_TIMELINE_V11.\n{\n  const DELAY_TICKS_V12 = ${delayTicks};\n  if (typeof window.__mwV11CommitBoundary !== "function" || typeof window.__mwV11ReadConfirmedTimeline !== "function") {\n    throw new Error("V12 requires confirmed timeline V11 controls");\n  }\n  const originalCommitBoundaryV12 = window.__mwV11CommitBoundary;\n  const state = { anchors: [], enabled: false, samples: [] };\n  const clone = (value) => Array.isArray(value) ? [...value] : null;\n  const lerp = (a, b, t) => [\n    a[0] + (b[0] - a[0]) * t,\n    a[1] + (b[1] - a[1]) * t,\n    a[2] + (b[2] - a[2]) * t,\n  ];\n  const distance = (a, b) => a && b ? Math.hypot(a[0] - b[0], a[1] - b[1], a[2] - b[2]) : null;\n\n  window.__mwV11CommitBoundary = (boundary, localBoundary) => {\n    const result = originalCommitBoundaryV12(boundary, localBoundary);\n    if (!result) return result;\n    const snapshot = window.__mwV11ReadConfirmedTimeline();\n    const commitment = [...(snapshot.commitments || [])].reverse().find((entry) => entry.boundary === boundary && Array.isArray(entry.position));\n    if (!commitment || state.anchors.some((entry) => entry.boundary === boundary)) return result;\n    state.anchors.push({ boundary, position: clone(commitment.position), committedAt: commitment.committedAt, lagTicks: commitment.lagTicks });\n    state.anchors.sort((a, b) => a.boundary - b.boundary);\n    if (state.anchors.length > 128) state.anchors.splice(0, state.anchors.length - 128);\n    return result;\n  };\n\n  window.__mwV12CommittedAnchorCount = () => state.anchors.length;\n  window.__mwV12ResetAll = () => { state.anchors = []; state.enabled = false; state.samples = []; return true; };\n  window.__mwV12ResetMetrics = () => { state.samples = []; return true; };\n  window.__mwV12Enable = () => { if (state.anchors.length < 3) return false; state.enabled = true; return true; };\n  window.__mwV12Disable = () => { state.enabled = false; return true; };\n\n  window.__mwV12ApplyRemotePresentation = (mesh, exactPosition) => {\n    if (!state.enabled || !mesh || state.anchors.length < 2) return false;\n    const localBoundary = Number.isInteger(localState?.boundaryTick) ? localState.boundaryTick : null;\n    if (!Number.isInteger(localBoundary)) return false;\n    const targetBoundary = localBoundary - DELAY_TICKS_V12;\n    const earliest = state.anchors[0];\n    const latest = state.anchors[state.anchors.length - 1];\n    let from = null;\n    let to = null;\n    let alpha = 0;\n    let presentationBoundary = targetBoundary;\n    let mode = "interpolate";\n\n    if (targetBoundary <= earliest.boundary) {\n      from = to = earliest;\n      presentationBoundary = earliest.boundary;\n      mode = "before-buffer";\n    } else if (targetBoundary >= latest.boundary) {\n      from = to = latest;\n      presentationBoundary = latest.boundary;\n      mode = targetBoundary === latest.boundary ? "latest-exact" : "underrun-hold";\n    } else {\n      for (let index = state.anchors.length - 1; index >= 1; index -= 1) {\n        const candidateTo = state.anchors[index];\n        const candidateFrom = state.anchors[index - 1];\n        if (candidateFrom.boundary <= targetBoundary && targetBoundary <= candidateTo.boundary) {\n          from = candidateFrom;\n          to = candidateTo;\n          alpha = (targetBoundary - from.boundary) / Math.max(1, to.boundary - from.boundary);\n          break;\n        }\n      }\n      if (!from || !to) {\n        from = to = latest;\n        presentationBoundary = latest.boundary;\n        mode = "anchor-gap-hold";\n      }\n    }\n\n    const displayed = from === to ? clone(from.position) : lerp(from.position, to.position, alpha);\n    mesh.position.fromArray(displayed);\n    syncPresence(mesh, displayed);\n    state.samples.push({\n      t: performance.now(),\n      delayTicks: DELAY_TICKS_V12,\n      localBoundary,\n      targetBoundary,\n      presentationBoundary,\n      newestCommittedBoundary: latest.boundary,\n      fromBoundary: from.boundary,\n      toBoundary: to.boundary,\n      alpha,\n      mode,\n      exactPosition: clone(exactPosition),\n      presentationPosition: clone(displayed),\n      spatialOffset: distance(displayed, exactPosition),\n    });\n    if (state.samples.length > 4096) state.samples.splice(0, state.samples.length - 4096);\n    return true;\n  };\n\n  window.__mwV12ReadPresentation = () => ({\n    delayTicks: DELAY_TICKS_V12,\n    enabled: state.enabled,\n    anchors: state.anchors.map((entry) => ({ ...entry, position: clone(entry.position) })),\n    samples: state.samples.map((entry) => ({ ...entry, exactPosition: clone(entry.exactPosition), presentationPosition: clone(entry.presentationPosition) })),\n  });\n}\n`;

writeFileSync(path, source);
console.log(`WORLD_V0_FIXED_DELAY_REMOTE_V12_INSTALLED delayTicks=${delayTicks}`);
