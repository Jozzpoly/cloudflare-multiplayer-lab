import { readFileSync, writeFileSync } from "node:fs";

const mode = String(process.argv[2] ?? "freeze");
if (!new Set(["baseline", "freeze"]).has(mode)) throw new Error(`V14 mode must be baseline|freeze, got ${mode}`);
const path = "public/world-v0/app.js";
const marker = "WORLD_V0_HISTORY_PRESSURE_V14";
let source = readFileSync(path, "utf8");
if (source.includes(marker)) {
  console.log("history-pressure v14 hook already installed");
  process.exit(0);
}

const guardBlock = `    if (silenceTicks >= safeBlindTicks) {\n      beginActorResume("authority_silence_history_guard", { silenceTicks, safeBlindTicks });\n      return;\n    }`;
if (!source.includes(guardBlock)) throw new Error("V14 install missing authority silence guard seam");
if (source.indexOf(guardBlock) !== source.lastIndexOf(guardBlock)) throw new Error("V14 install ambiguous authority silence guard seam");

const baselineBody = `    const pressureV14 = silenceTicks >= safeBlindTicks;\n    if (typeof window.__mwV14ObserveHistoryPressure === "function") {\n      window.__mwV14ObserveHistoryPressure({ mode: "baseline", pressure: pressureV14, silenceTicks, safeBlindTicks, localBoundary: localState.boundaryTick, authorityBoundary: lastAuthorityBoundaryTick });\n    }\n    if (pressureV14) {\n      beginActorResume("authority_silence_history_guard", { silenceTicks, safeBlindTicks });\n      return;\n    }`;
const freezeBody = `    const pressureV14 = silenceTicks >= safeBlindTicks;\n    if (typeof window.__mwV14ObserveHistoryPressure === "function") {\n      window.__mwV14ObserveHistoryPressure({ mode: "freeze", pressure: pressureV14, silenceTicks, safeBlindTicks, localBoundary: localState.boundaryTick, authorityBoundary: lastAuthorityBoundaryTick });\n    }\n    if (pressureV14) {\n      // ${marker}: history pressure is not transport/session loss. Hold only further\n      // speculative advancement while the live authority stream catches up.\n      return;\n    }`;
source = source.replace(guardBlock, mode === "freeze" ? freezeBody : baselineBody);

source += `\n// ${marker} public test controls. Canonical app.js is restored after CI.\n{\n  const MODE_V14 = ${JSON.stringify(mode)};\n  const state = {\n    observations: 0,\n    pressureFrames: 0,\n    episodes: [],\n    activeEpisode: null,\n    maxSilenceTicks: 0,\n    minSafeBlindTicks: null,\n    firstPressureAt: null,\n    lastPressureAt: null,\n  };\n  const closeEpisode = (now, sample) => {\n    if (!state.activeEpisode) return;\n    state.activeEpisode.endAt = now;\n    state.activeEpisode.durationMs = now - state.activeEpisode.startAt;\n    state.activeEpisode.endLocalBoundary = sample?.localBoundary ?? null;\n    state.activeEpisode.endAuthorityBoundary = sample?.authorityBoundary ?? null;\n    state.episodes.push(state.activeEpisode);\n    if (state.episodes.length > 256) state.episodes.splice(0, state.episodes.length - 256);\n    state.activeEpisode = null;\n  };\n  window.__mwV14ObserveHistoryPressure = (sample) => {\n    const now = performance.now();\n    state.observations += 1;\n    const silenceTicks = Number(sample?.silenceTicks ?? 0);\n    const safeBlindTicks = Number(sample?.safeBlindTicks ?? 0);\n    state.maxSilenceTicks = Math.max(state.maxSilenceTicks, silenceTicks);\n    if (Number.isFinite(safeBlindTicks)) state.minSafeBlindTicks = state.minSafeBlindTicks === null ? safeBlindTicks : Math.min(state.minSafeBlindTicks, safeBlindTicks);\n    if (sample?.pressure) {\n      state.pressureFrames += 1;\n      if (state.firstPressureAt === null) state.firstPressureAt = now;\n      state.lastPressureAt = now;\n      if (!state.activeEpisode) {\n        state.activeEpisode = {\n          startAt: now,\n          startLocalBoundary: sample.localBoundary ?? null,\n          startAuthorityBoundary: sample.authorityBoundary ?? null,\n          safeBlindTicks,\n          maxSilenceTicks: silenceTicks,\n          frames: 0,\n        };\n      }\n      state.activeEpisode.frames += 1;\n      state.activeEpisode.maxSilenceTicks = Math.max(state.activeEpisode.maxSilenceTicks, silenceTicks);\n      state.activeEpisode.lastLocalBoundary = sample.localBoundary ?? null;\n      state.activeEpisode.lastAuthorityBoundary = sample.authorityBoundary ?? null;\n    } else {\n      closeEpisode(now, sample);\n    }\n    return true;\n  };\n  window.__mwV14ResetHistoryPressure = () => {\n    state.observations = 0;\n    state.pressureFrames = 0;\n    state.episodes = [];\n    state.activeEpisode = null;\n    state.maxSilenceTicks = 0;\n    state.minSafeBlindTicks = null;\n    state.firstPressureAt = null;\n    state.lastPressureAt = null;\n    return true;\n  };\n  window.__mwV14ReadHistoryPressure = () => {\n    const now = performance.now();\n    const episodes = state.episodes.map((entry) => ({ ...entry }));\n    if (state.activeEpisode) episodes.push({ ...state.activeEpisode, endAt: now, durationMs: now - state.activeEpisode.startAt, active: true });\n    return {\n      mode: MODE_V14,\n      observations: state.observations,\n      pressureFrames: state.pressureFrames,\n      pressureRatio: state.observations ? state.pressureFrames / state.observations : 0,\n      episodes,\n      active: Boolean(state.activeEpisode),\n      maxSilenceTicks: state.maxSilenceTicks,\n      minSafeBlindTicks: state.minSafeBlindTicks,\n      firstPressureAt: state.firstPressureAt,\n      lastPressureAt: state.lastPressureAt,\n    };\n  };\n}\n`;

writeFileSync(path, source);
console.log(`WORLD_V0_HISTORY_PRESSURE_V14_INSTALLED mode=${mode}`);
