import { readFileSync, writeFileSync } from "node:fs";

const path = "public/world-v0/app.js";
const marker = "WORLD_V0_ORDERED_INBOUND_DELAY_V17";
let source = readFileSync(path, "utf8");
if (source.includes(marker)) {
  console.log("ordered inbound delay v17 hook already installed");
  process.exit(0);
}

function replaceOnce(from, to, label) {
  const first = source.indexOf(from);
  if (first < 0) throw new Error(`V17 ordered-delay install missing seam: ${label}`);
  if (source.indexOf(from, first + from.length) >= 0) throw new Error(`V17 ordered-delay install ambiguous seam: ${label}`);
  source = source.slice(0, first) + to + source.slice(first + from.length);
}

const canonicalHandler = `  connection.addEventListener("message", (event) => {\n    if (socket !== connection || typeof event.data !== "string") return;\n    try {\n      const message = JSON.parse(event.data);\n      const observedBoundary = Number.isInteger(message?.boundaryTick)\n        ? message.boundaryTick\n        : (Number.isInteger(message?.state?.boundaryTick) ? message.state.boundaryTick : null);\n      if (Number.isInteger(observedBoundary)) {\n        lastAuthorityBoundaryTick = lastAuthorityBoundaryTick === null\n          ? observedBoundary\n          : Math.max(lastAuthorityBoundaryTick, observedBoundary);\n        metrics.latestAuthorityBoundary = lastAuthorityBoundaryTick;\n      }\n      handleMessage(message);\n      updateProductStatus();\n    } catch (error) { candidateError(error); }\n  });`;

const instrumentedHandler = `  connection.addEventListener("message", (event) => {\n    if (typeof event.data !== "string") return;\n    const raw = event.data;\n    const deliver = () => {\n      if (socket !== connection) {\n        if (typeof window.__mwV17OrderedInboundDropped === "function") window.__mwV17OrderedInboundDropped(raw);\n        return;\n      }\n      try {\n        const message = JSON.parse(raw);\n        const observedBoundary = Number.isInteger(message?.boundaryTick)\n          ? message.boundaryTick\n          : (Number.isInteger(message?.state?.boundaryTick) ? message.state.boundaryTick : null);\n        if (Number.isInteger(observedBoundary)) {\n          lastAuthorityBoundaryTick = lastAuthorityBoundaryTick === null\n            ? observedBoundary\n            : Math.max(lastAuthorityBoundaryTick, observedBoundary);\n          metrics.latestAuthorityBoundary = lastAuthorityBoundaryTick;\n        }\n        handleMessage(message);\n        updateProductStatus();\n        if (typeof window.__mwV17OrderedInboundDelivered === "function") window.__mwV17OrderedInboundDelivered(raw, observedBoundary);\n      } catch (error) { candidateError(error); }\n    };\n    if (socket !== connection) return;\n    if (typeof window.__mwV17ScheduleOrderedInbound === "function" && window.__mwV17ScheduleOrderedInbound(deliver, raw)) return;\n    deliver();\n  });`;

replaceOnce(canonicalHandler, instrumentedHandler, "ordered WebSocket message delivery");

source += `\n// ${marker}: test-only receive-side FIFO delay. Every inbound WebSocket text frame shares\n// one ordered queue; no pong/ACK/state/peer frame can overtake an earlier queued frame.\n{\n  const queue = [];\n  let delayMs = 0;\n  let timer = null;\n  let nextSequence = 1;\n  let lastDeliveredSequence = 0;\n  const state = {\n    scheduled: 0,\n    delivered: 0,\n    dropped: 0,\n    orderViolations: 0,\n    maxQueueDepth: 0,\n    maxActualDelayMs: 0,\n    byType: {},\n    events: [],\n  };\n  const metadata = (raw) => {\n    try {\n      const message = JSON.parse(raw);\n      return {\n        type: String(message?.type || "unknown"),\n        boundaryTick: Number.isInteger(message?.boundaryTick) ? message.boundaryTick : null,\n        targetTick: Number.isInteger(message?.targetTick) ? message.targetTick : null,\n      };\n    } catch {\n      return { type: "parse-error", boundaryTick: null, targetTick: null };\n    }\n  };\n  const retain = (entry) => {\n    state.events.push(entry);\n    if (state.events.length > 320) state.events.splice(0, state.events.length - 320);\n  };\n  const schedulePump = () => {\n    if (timer !== null || !queue.length) return;\n    const waitMs = Math.max(0, queue[0].dueAt - performance.now());\n    timer = setTimeout(pump, waitMs);\n  };\n  const pump = () => {\n    timer = null;\n    let now = performance.now();\n    while (queue.length && queue[0].dueAt <= now) {\n      const item = queue.shift();\n      if (item.sequence !== lastDeliveredSequence + 1) state.orderViolations += 1;\n      lastDeliveredSequence = item.sequence;\n      state.maxActualDelayMs = Math.max(state.maxActualDelayMs, now - item.enqueuedAt);\n      item.deliver();\n      now = performance.now();\n    }\n    schedulePump();\n  };\n  window.__mwV17SetOrderedInboundDelay = (value) => {\n    delayMs = Math.max(0, Number(value) || 0);\n    retain({ kind: "set-delay", at: performance.now(), delayMs, queueDepth: queue.length });\n    schedulePump();\n    return delayMs;\n  };\n  window.__mwV17ResetOrderedInbound = () => {\n    if (queue.length) throw new Error(`V17 reset with non-empty queue ${queue.length}`);\n    state.scheduled = 0;\n    state.delivered = 0;\n    state.dropped = 0;\n    state.orderViolations = 0;\n    state.maxQueueDepth = 0;\n    state.maxActualDelayMs = 0;\n    state.byType = {};\n    state.events = [];\n    return true;\n  };\n  window.__mwV17ScheduleOrderedInbound = (deliver, raw) => {\n    if (!(delayMs > 0) && queue.length === 0) return false;\n    const meta = metadata(raw);\n    const sequence = nextSequence++;\n    const enqueuedAt = performance.now();\n    queue.push({\n      sequence,\n      enqueuedAt,\n      dueAt: enqueuedAt + delayMs,\n      deliver,\n      meta,\n    });\n    state.scheduled += 1;\n    state.byType[meta.type] = (state.byType[meta.type] || 0) + 1;\n    state.maxQueueDepth = Math.max(state.maxQueueDepth, queue.length);\n    if (state.scheduled <= 12 || state.scheduled % 100 === 0) retain({ kind: "scheduled", at: enqueuedAt, sequence, delayMs, queueDepth: queue.length, ...meta });\n    schedulePump();\n    return true;\n  };\n  window.__mwV17OrderedInboundDelivered = (raw, observedBoundary) => {\n    state.delivered += 1;\n    const meta = metadata(raw);\n    if (state.delivered <= 12 || state.delivered % 100 === 0) retain({ kind: "delivered", at: performance.now(), observedBoundary, queueDepth: queue.length, ...meta });\n    return true;\n  };\n  window.__mwV17OrderedInboundDropped = (raw) => {\n    state.dropped += 1;\n    retain({ kind: "dropped", at: performance.now(), queueDepth: queue.length, ...metadata(raw) });\n    return true;\n  };\n  window.__mwV17ReadOrderedInbound = () => ({\n    delayMs,\n    queueDepth: queue.length,\n    nextSequence,\n    lastDeliveredSequence,\n    ...state,\n    byType: { ...state.byType },\n    events: state.events.map((entry) => ({ ...entry })),\n  });\n}\n`;

writeFileSync(path, source);
console.log("WORLD_V0_ORDERED_INBOUND_DELAY_V17_INSTALLED");
