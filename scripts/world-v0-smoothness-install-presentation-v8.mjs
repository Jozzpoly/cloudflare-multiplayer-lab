import { readFileSync, writeFileSync } from "node:fs";

const path = "public/world-v0/app.js";
const marker = "WORLD_V0_PRESENTATION_V8_BOUNDED_REMOTE_CORRECTION_DEBT";
const halfLifeMs = Number(process.argv[2] || process.env.MW_WORLD_V0_PRESENTATION_HALF_LIFE_MS || 50);
const capTicks = Number(process.argv[3] || process.env.MW_WORLD_V0_PRESENTATION_CAP_TICKS || 1);
if (!Number.isFinite(halfLifeMs) || halfLifeMs <= 0) throw new Error(`invalid V8 half-life ${halfLifeMs}`);
if (!Number.isFinite(capTicks) || capTicks <= 0) throw new Error(`invalid V8 cap ticks ${capTicks}`);

let source = readFileSync(path, "utf8");
if (source.includes(marker)) {
  console.log("presentation v8 already installed");
  process.exit(0);
}

const syncNeedle = `function syncMeshes() {`;
if (!source.includes(syncNeedle)) throw new Error("V8 could not find syncMeshes seam");

const helpers = `// ${marker} — test-only presentation experiment. Canonical source is restored after CI.\nconst __mwV8HalfLifeMs = ${JSON.stringify(halfLifeMs)};\nconst __mwV8CapTicks = ${JSON.stringify(capTicks)};\nconst __mwV8Presentation = {\n  sessionId: null,\n  offset: [0, 0, 0],\n  pendingRecovery: [0, 0, 0],\n  lastAt: performance.now(),\n  injections: [],\n  syncs: [],\n};\nfunction __mwV8VecAdd(a, b) { return [a[0] + b[0], a[1] + b[1], a[2] + b[2]]; }\nfunction __mwV8VecSub(a, b) { return [a[0] - b[0], a[1] - b[1], a[2] - b[2]]; }\nfunction __mwV8VecScale(a, scalar) { return [a[0] * scalar, a[1] * scalar, a[2] * scalar]; }\nfunction __mwV8VecMagnitude(a) { return Math.hypot(a[0], a[1], a[2]); }\nfunction __mwV8CapMeters() {\n  const speed = Number(simulation?.movement?.playerSpeed);\n  return Number.isFinite(speed) && speed > 0 ? speed * FIXED_DT * __mwV8CapTicks : null;\n}\nfunction __mwV8ClampMagnitude(vector, capMeters) {\n  const magnitude = __mwV8VecMagnitude(vector);\n  if (!Number.isFinite(capMeters) || capMeters <= 0 || magnitude <= capMeters || magnitude <= 1e-12) return [...vector];\n  return __mwV8VecScale(vector, capMeters / magnitude);\n}\nfunction __mwV8ResetForSession(sessionId = remoteSessionId) {\n  __mwV8Presentation.sessionId = sessionId || null;\n  __mwV8Presentation.offset = [0, 0, 0];\n  __mwV8Presentation.pendingRecovery = [0, 0, 0];\n  __mwV8Presentation.lastAt = performance.now();\n}\nfunction __mwV8EnsureSession() {\n  if (__mwV8Presentation.sessionId !== (remoteSessionId || null)) __mwV8ResetForSession(remoteSessionId);\n}\nfunction __mwV8DecayTo(now) {\n  const dtMs = Math.max(0, now - __mwV8Presentation.lastAt);\n  if (dtMs <= 0) return;\n  const before = __mwV8Presentation.offset;\n  const factor = Math.pow(0.5, dtMs / __mwV8HalfLifeMs);\n  const after = __mwV8VecScale(before, factor);\n  const recovery = __mwV8VecSub(after, before);\n  __mwV8Presentation.offset = after;\n  __mwV8Presentation.pendingRecovery = __mwV8VecAdd(__mwV8Presentation.pendingRecovery, recovery);\n  __mwV8Presentation.lastAt = now;\n}\nfunction __mwV8ApplyRemoteCorrection(before, after, reason, targetTick, boundaryBefore) {\n  __mwV8EnsureSession();\n  const beforePosition = before.actors.get(remoteSessionId)?.position;\n  const afterPosition = after.actors.get(remoteSessionId)?.position;\n  if (!beforePosition || !afterPosition) return;\n  const now = performance.now();\n  __mwV8DecayTo(now);\n  const exactCorrection = __mwV8VecSub(afterPosition, beforePosition);\n  if (__mwV8VecMagnitude(exactCorrection) <= 1e-12) return;\n  const capMeters = __mwV8CapMeters();\n  if (!Number.isFinite(capMeters) || capMeters <= 0) throw new Error(\"V8 physical presentation cap unavailable\");\n  const offsetBeforeInjection = [...__mwV8Presentation.offset];\n  const presentedBefore = __mwV8VecAdd(beforePosition, offsetBeforeInjection);\n  const unboundedOffset = __mwV8VecSub(offsetBeforeInjection, exactCorrection);\n  const boundedOffset = __mwV8ClampMagnitude(unboundedOffset, capMeters);\n  const clampedAway = __mwV8VecSub(unboundedOffset, boundedOffset);\n  __mwV8Presentation.offset = boundedOffset;\n  const presentedAfter = __mwV8VecAdd(afterPosition, boundedOffset);\n  const continuityResidual = __mwV8VecSub(presentedAfter, presentedBefore);\n  __mwV8Presentation.injections.push({\n    t: now, reason, targetTick, boundaryBefore, capTicks: __mwV8CapTicks, capMeters,\n    exactCorrection: [...exactCorrection],\n    offsetBeforeInjection,\n    unboundedOffset,\n    offsetAfterInjection: [...boundedOffset],\n    clampedAway,\n    clamped: __mwV8VecMagnitude(clampedAway) > 1e-9,\n    continuityResidual,\n    continuityResidualMagnitude: __mwV8VecMagnitude(continuityResidual),\n  });\n  if (__mwV8Presentation.injections.length > 2048) __mwV8Presentation.injections.splice(0, __mwV8Presentation.injections.length - 2048);\n}\nfunction __mwV8PresentedRemotePosition(exactPosition) {\n  __mwV8EnsureSession();\n  const now = performance.now();\n  __mwV8DecayTo(now);\n  const recoveryVector = [...__mwV8Presentation.pendingRecovery];\n  __mwV8Presentation.pendingRecovery = [0, 0, 0];\n  const presented = __mwV8VecAdd(exactPosition, __mwV8Presentation.offset);\n  const capMeters = __mwV8CapMeters();\n  __mwV8Presentation.syncs.push({\n    t: now, boundaryTick: localState?.boundaryTick ?? null, capTicks: __mwV8CapTicks, capMeters,\n    exactPosition: [...exactPosition],\n    presentedPosition: [...presented],\n    offset: [...__mwV8Presentation.offset],\n    offsetMagnitude: __mwV8VecMagnitude(__mwV8Presentation.offset),\n    recoveryVector,\n    recoveryMagnitude: __mwV8VecMagnitude(recoveryVector),\n  });\n  if (__mwV8Presentation.syncs.length > 4096) __mwV8Presentation.syncs.splice(0, __mwV8Presentation.syncs.length - 4096);\n  return presented;\n}\nwindow.__mwV8ResetPresentation = () => {\n  __mwV8Presentation.injections = [];\n  __mwV8Presentation.syncs = [];\n  __mwV8ResetForSession(remoteSessionId);\n  return true;\n};\nwindow.__mwV8ReadPresentation = () => ({\n  revision: \"world-v0-presentation-v8-bounded-remote-correction-debt\",\n  halfLifeMs: __mwV8HalfLifeMs,\n  capTicks: __mwV8CapTicks,\n  capMeters: __mwV8CapMeters(),\n  sessionId: __mwV8Presentation.sessionId,\n  offset: [...__mwV8Presentation.offset],\n  injections: __mwV8Presentation.injections.map((event) => ({ ...event, exactCorrection: [...event.exactCorrection], offsetBeforeInjection: [...event.offsetBeforeInjection], unboundedOffset: [...event.unboundedOffset], offsetAfterInjection: [...event.offsetAfterInjection], clampedAway: [...event.clampedAway], continuityResidual: [...event.continuityResidual] })),\n  syncs: __mwV8Presentation.syncs.map((event) => ({ ...event, exactPosition: [...event.exactPosition], presentedPosition: [...event.presentedPosition], offset: [...event.offset], recoveryVector: [...event.recoveryVector] })),\n});\n\n`;
source = source.replace(syncNeedle, helpers + syncNeedle);

const correctionNeedle = `  const after = captureDynamic(localState.sim);\n  const delta = correctionDelta(before, after);`;
if (!source.includes(correctionNeedle)) throw new Error("V8 could not find correction seam");
source = source.replace(
  correctionNeedle,
  `${correctionNeedle}\n  __mwV8ApplyRemoteCorrection(before, after, reason, targetTick, currentBoundary);`,
);

const remoteNeedle = `  if (remoteBody && remoteMesh) {\n    const position = bodyPosition(remoteBody);\n    remoteMesh.position.fromArray(position);\n    remoteMesh.quaternion.fromArray(bodyRotation(remoteBody)).normalize();\n    syncPresence(remoteMesh, position);\n  }`;
if (!source.includes(remoteNeedle)) throw new Error("V8 could not find remote presentation seam");
source = source.replace(
  remoteNeedle,
  `  if (remoteBody && remoteMesh) {\n    const position = bodyPosition(remoteBody);\n    const presentedPosition = __mwV8PresentedRemotePosition(position);\n    remoteMesh.position.fromArray(presentedPosition);\n    remoteMesh.quaternion.fromArray(bodyRotation(remoteBody)).normalize();\n    syncPresence(remoteMesh, presentedPosition);\n  }`,
);

writeFileSync(path, source);
console.log(`WORLD_V0_PRESENTATION_V8_INSTALLED halfLifeMs=${halfLifeMs} capTicks=${capTicks}`);
